import getCameraStream from "./getCameraStream";
import playCameraStream from "./playCameraStream";
import videoToCanvas from "./videoToCanvas";

class WebcamTools {
  getCameraStream = getCameraStream
  playCameraStream = playCameraStream
  videoToCanvas = videoToCanvas
}

export default WebcamTools;